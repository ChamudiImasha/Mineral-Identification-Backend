[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend/app && python -m uvicorn api_server:app --host 0.0.0.0 --port $PORT"
