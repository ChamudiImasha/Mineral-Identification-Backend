[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install --no-cache-dir -r backend/app/requirements_api.txt"]

[start]
cmd = "cd backend/app && python -m uvicorn api_server:app --host 0.0.0.0 --port $PORT"
