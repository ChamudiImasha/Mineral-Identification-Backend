# Optimized requirements for Railway deployment
# CPU-only PyTorch to reduce Docker image size from 2GB to ~850MB

# Core ML Dependencies - CPU ONLY
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.0+cpu
torchvision==0.15.0+cpu

# CLIP for planetary image gatekeeper
open-clip-torch>=2.20.0

# Scientific Computing - pin numpy version for PyTorch CPU compatibility
numpy==1.24.3
scipy>=1.10.0

# Data Handling
h5py>=3.8.0
matplotlib>=3.6.0

# Computer Vision - headless for Docker
opencv-python-headless>=4.8.0
Pillow>=9.0.0

# Logging
colorlog>=6.7.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
gunicorn>=21.2.0
python-multipart>=0.0.6
requests>=2.31.0

# Model Download
gdown>=4.6.0
